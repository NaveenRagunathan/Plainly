# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/plainly

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Email Provider (Resend) - Get key from https://resend.com
RESEND_API_KEY=re_xxxxx
FROM_EMAIL=onboarding@resend.dev
FROM_NAME=Plainly

# Redis for job queue (optional)
REDIS_URL=redis://localhost:6379

# Stripe (optional) - Get keys from https://stripe.com
STRIPE_SECRET_KEY=sk_test_xxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# App URL (for tracking links and redirects)
APP_URL=http://localhost:5000
